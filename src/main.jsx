import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './SmartHomeDashboard.jsx' // Note the change here to import your file

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)

